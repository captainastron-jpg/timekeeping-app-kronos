{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm run build && npm ci --prefix server"
  },
  "deploy": {
    "startCommand": "node server/index.js",
    "restartPolicyType": "always",
    "restartPolicyMaxRetries": 10
  }
}
